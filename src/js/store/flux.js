const getState = ({ getStore, getActions, setStore }) => {
    return {
        // getStore {Function} - Función que devuelve el estado actual de la aplicación.
        // getActions {Function} - Función que devuelve las acciones disponibles en la aplicación.
        // setStore {Function} - Función que actualiza el estado de la aplicación.
        store: {},
        actions: {},
    }
}

export default getState
